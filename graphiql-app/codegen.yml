overwrite: true
schema: 'https://countries.trevorblades.com/graphql'
documents: null
generates:
  src/generated/graphql-types.tsx:
    plugins:
      - 'typescript'
# - "typescript-resolvers"

# - 'typescript-common'
